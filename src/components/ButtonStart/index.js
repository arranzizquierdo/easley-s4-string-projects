import React, { Component } from 'react';
import './ButtonStart.scss';

class ButtonStart extends Component {
    render() {
        return (
            <button className='button__styles'>Inicia sesión</button>
        );
    }
}

export default ButtonStart;
